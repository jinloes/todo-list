import { TaskHttpInterceptor } from './task-http-interceptor.service';

describe('TaskHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new TaskHttpInterceptor()).toBeTruthy();
  });
});
